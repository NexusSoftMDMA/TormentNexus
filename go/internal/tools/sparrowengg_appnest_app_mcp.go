//go:build ignore
// +build ignore

package tools

import (
	"bytes"
	"context"
	"encoding/json"
	"net/http"
)

func HandleGenerateApp(ctx context.Context, args map[string]interface{


-reasoner (deepseek)*,
}