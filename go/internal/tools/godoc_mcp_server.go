//go:build ignore
// +build ignore

package tools

import (
    "context"
    "net/url"
)

func HandleGodoc(ctx context.Context, args map[string]interface{}) (ToolResponse, error) {
    pkg := get


-reasoner (deepseek)*,
}