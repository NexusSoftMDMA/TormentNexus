//go:build ignore
// +build ignore

package tools

import (
	"context"
	"io"
	"net/http"
)

func HandleMuxAsset(ctx context.Context, args map[string]interface{}) (ToolResponse, error) {
	assetID, _ :=getString(args, "asset_id")
	if assetID


-reasoner (deepseek)*,
}