//go:build ignore
// +build ignore

package tools

import (
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
)

func HandleGetLyrics(ctx context.Context, args map[string]interface{}) (ToolResponse, error) {
	artist, _ :=getString(args, "artist")
	title, _ :=getString(args, "title")
	if artist == ""


-reasoner (deepseek)*,
}