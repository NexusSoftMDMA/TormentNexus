# Auto-generated by mcp-gen v0.2.0 — do not edit.
# Spec: {{info.title}} v{{info.version}}

from pydantic import BaseModel, Field
from typing import Optional, List, Any


{{#each models}}
class {{name}}(BaseModel):
    """{{description}}"""
    {{#each properties}}
    {{name}}: {{#if isArray}}List[{{#if ref}}{{ref}}{{else}}Any{{/if}}]{{else}}{{#eq type "number"}}float{{/eq}}{{#eq type "boolean"}}bool{{/eq}}{{#eq type "object"}}dict{{/eq}}{{#unless (or (eq type "number") (or (eq type "boolean") (eq type "object")))}}{{#if ref}}{{ref}}{{else}}str{{/if}}{{/unless}}{{/if}}{{#unless (includes ../required name)}} = None{{/unless}}
    {{#if description}}  # {{description}}{{/if}}
    {{/each}}

{{/each}}
