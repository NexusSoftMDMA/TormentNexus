---
description: MCP | Show the CTX MCP stdio launch command
---

Show the CTX MCP stdio launch command for the current repository.

Use the current repository root and explain how a host CLI can launch `'/Users/alessandrogautieri/Documents/coding/ctx/target/debug/ctx' --repo-root 'demo/fixtures/opencode-auth-lab' mcp stdio` locally.
