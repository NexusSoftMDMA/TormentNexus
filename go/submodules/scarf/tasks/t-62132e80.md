---
id: t-62132e80
title: B2: profile-scope chat ACP + scoped CLI invocations via -p flag
status: done
added: 2026-06-25
priority: high
---

## Description

Parent: t-873f7df9 (#120 Design B). Makes the CLI/process layer (ACP chat + profile-scoped hermes commands) operate against the selected profile. Source-verified: `-p <name>` (incl. `-p default`) forces the profile regardless of host active_profile.

## Plan

## Plan
1. ACP chat command (ACPClient+iOS.swift ~L72): insert `-p <selectedProfile>` before `acp` → `hermes -p <name> acp`. Use the selected profile name (or "default").
2. Centralize a helper that builds profile-scoped hermes argv given context+selection; apply to iOS CLI reads that should be profile-scoped (skills browse, curator, profile show, etc.). Leave `hermes profile list` ROOT-scoped (lists all; -p harmless but unnecessary).
3. Do NOT inject into non-hermes commands (e.g. `echo $HOME` probe in UserHomeCache).

## Tests
- Command construction includes `-p <name>` for chat + scoped CLI; default→`-p default`; absent selection→no scoping change.
- Unit: argv builder for named + default.

## Audit
- Fresh-eyes: verify against Hermes main.py parser that `-p` before `acp` is consumed correctly; confirm no double-injection; confirm `-p default` forces default (defeats host active_profile). Re-verify env vs flag precedence note from epic.

## Commit
- `feat(ios,profiles): scope chat + CLI to selected profile via -p flag (#120)`

## Artifacts

Commit 6ff7739.
- ScarfCore HermesProfileScope: isProfileHome(_:) + hermesHomeShellAssignment(forHome:) (+ private shellQuoteHome mirroring RemoteSQLiteBackend.quoteForRemoteShell). Named-profile home → `HERMES_HOME="$HOME/..."` (escaped) / `'...'` for absolutes; default/root → "".
- ScarfIOS CitadelServerTransport.asyncRunProcess: prepends the assignment to ALL runProcess commands (covers direct hermesBinary calls AND `/bin/sh -c "...hermes..."` wrappers in Memory/Chat/Settings; harmless for echo $HOME). streamScript left unscoped (sqlite-only, verified).
- ScarfIOS ACPClient+iOS: prepends to `exec hermes acp`.

Mechanism choice: HERMES_HOME (not -p) — matches B1's remoteHome seam, version-stable, default=no-injection preserves legacy/active_profile + pre-profile-host compat. Verified vs Hermes 0.16 (main.py early-returns trusting a profiles/<name> HERMES_HOME).

Tests: ScarfCore swift-test 21 pass (assignment/quoting/tilde/metachar-injection neutralization + isProfileHome edges). iOS BUILD SUCCEEDED on 26.5 sim.

Fresh-eyes audit (subagent, security-focused): NO Critical. Verified env propagation through sh-wrappers, exec survival, default byte-identical, streamScript=sqlite-only, full runProcess coverage. Fixed S1 (escaping parity with quoteForRemoteShell) + S2 (injection/edge tests). NTH1: Mac ACP intentionally not scoped (Design B is iOS-only).

