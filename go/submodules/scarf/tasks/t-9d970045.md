---
id: t-9d970045
title: B3: profile switcher UI in ScarfGo ProfilesView
status: done
added: 2026-06-25
priority: high
---

## Description

Parent: t-873f7df9 (#120 Design B). User-facing switcher: turn the read-only Profiles list into a selectable picker, persist via B0, trigger B1 rebuild, restart live chat, and make the active scope visible.

## Plan

## Plan
1. Make profile rows selectable (tap → confirm dialog → set B0 selection). Show a checkmark/badge on the currently-SELECTED (viewing) profile distinct from the host's active_profile badge.
2. Persistent "Viewing: <profile>" indicator (Profiles header + consider a subtle global cue).
3. Rewrite the read-only footer/disclaimer copy: phone now scopes its OWN view (HERMES_HOME) and does NOT change the host's active profile; create/delete/import-export still Mac-only.
4. Ensure live ACP chat restarts on switch (ChatView intentionally doesn't tear down on disappear — force teardown/restart on profile change, dovetails with B1 `.id`).
5. Localize new strings (Localizable.xcstrings, 7 locales — English now, others can follow project i18n flow).

## Tests
- Selecting a row updates + persists selection; confirm cancel = no-op.
- View reflects selected vs host-active distinctly.
- Chat session restarts (state machine) on switch.

## Audit
- Fresh-eyes: no accidental destructive verb wired; default selection reachable; a11y (Dynamic Type clamp, labels); copy accurate vs implementation.

## Commit
- `feat(ios,profiles): profile switcher UI with per-connection scoping (#120)`

## Artifacts

Commit 788060d.
- ProfilesView: rewritten read-only → selectable switcher. Rows (Default + named) tappable → confirm → coordinator.setSelectedProfile; checkmark + "Viewing" badge on the selected; server's-active-profile read from rootHome(active_profile) and surfaced only when it diverges. Default always reachable. Confirm copy clarifies the host's active profile is unchanged.
- HermesProfileScope.rootHome(forHome:) (+ edge tests): strips /profiles/<name> → root for root-only reads.
- ChatController `isolated deinit`: cancels tasks + fire-and-forget client.stop() → tears down old `hermes acp`+SSH on switch/disconnect/forget (fixes leak the code's comment assumed existed). Required Swift 6.1+ isolated-deinit (app target is Swift 6 strict).

Verification: ScarfCore swift-test 25 pass. iOS BUILD SUCCEEDED + smoke-launched on 26.5 sim (no crash; lands on onboarding — full switcher needs a live host). New UI strings are English literals pending i18n pass.

Fresh-eyes audit (subagent): NO Critical (isolated-deinit + leak-fix verified). Fixed S1 (doc profile-list scope-independence), S2 (no false "couldn't reach" on zero-profile hosts), S3 (clear pendingChoice on confirm), N3/N4 (a11y label + .isSelected trait), N5 (rootHome edge tests). N1 documented (confirm action must stay fire-and-forget). Deferred B1 audit items #2 (show selected not host-active) and #3 (read active_profile from root) are RESOLVED here.

B4 manual checklist (real host): switch mid-stream → old hermes acp PID dies + new spawns scoped; host active_profile unchanged after switch (cat before/after); zero-profile host shows footer not error; VoiceOver pass.

